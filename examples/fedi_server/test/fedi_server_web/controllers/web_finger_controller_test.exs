defmodule FediServerWeb.WebFingerControllerTest do
  use FediServerWeb.ConnCase

  test "GET /.well-known/host-meta", %{conn: conn} do
  end

  test "GET /.well-known/webfinger", %{conn: conn} do
  end
end
